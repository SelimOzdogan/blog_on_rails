<% if @post.errors.any? %>
<ul>
    <% @post.errors.full_messages.each do |msg| %>
    <li><%= msg %></li>
    <% end %>
</ul>

<%= form_for @post do |p| %>
<div class="form-group">
<% p.label :title %>
<% p.text_field :title, class: "form-control" %>
</div>
<% end %>